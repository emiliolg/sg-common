include ":common-core",
        ":common-env",
        ":common-service",
        ":common-ext",
        ":common-websocket",
        ":common-gwtutils",
        ":common-test",
		":common-gwt",
		":common-all"

// rootProject.children.each { it.name = rootProject.name + "-"+it.name }
